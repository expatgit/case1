
{% extends 'guest/base.html.twig' %}

{#{% block title %}
    {{ title }}

{% endblock %}#}

{% block body %}

    <div class="container-xl">
        <div class="row no-gutters justify-content-center mt-5">
            {% if app.user %}
            <div class="col-12 col-lg-6 col-md-6">
                <h1 class="display-4 text-center ">
                    Добро пожаловать, {{ app.user.address }}!
                </h1>

            </div>
            {% else %}
            <div class="col-12 col-lg-6 col-md-6">
                <h1 class="display-4 text-center">
                    {{ title }}
                </h1>

            </div>
            {% endif %}
        </div>
    </div>




{% endblock %}
